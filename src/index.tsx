import React from 'react';
import ReactDOM from 'react-dom';

import { Cadastro } from './pages/Cadastro';

ReactDOM.render(
  <React.StrictMode>
    <Cadastro children={undefined} />
  </React.StrictMode>,
  document.getElementById('root')
);
